# 复制为 .env 后填写真实值；不要将含真实密码的 .env 提交到 Git

# 部署目标服务器（SSH 密码登录）
DEPLOY_HOST=
DEPLOY_USER=root
DEPLOY_PASSWORD=
# 服务器上 doctor-dog-web 仓库的绝对路径（nginx.conf 里各 app 的 dist 路径以此为基准）
DEPLOY_PATH=/lihong/code/doctor-dog-web
